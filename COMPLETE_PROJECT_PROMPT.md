# COMPLETE_PROJECT_PROMPT

Recreate CLI with exports, sync flags, validate, and integrations (Copilot/Cursor/Gemini/OpenCode).


## Estensioni richieste (questa versione FULL)
- Popola tutte le guide enterprise elencate (global, Laravel, TS/Hono, CF Workers, RN).
- Aggiungi agenti SSOT: task-router, docs-writer, test-writer, dto-builder.
- CLI: aggiungi flag `--cursor-split` a `ai sync --cursor-here` per generare `.cursor/rules/ai-standards-*.mdc` per categoria.
- `ai sync --opencode-here` deve **sintetizzare dinamicamente** i sub-agenti `.opencode/agent/*.md` a partire dalla SSOT (Claude agents), senza doverli mantenere nel repo.
- README: inserisci **al principio** la struttura cartelle completa con `#` commenti; inserisci **Esempi rapidi d’uso**; inserisci i **link ufficiali** alle doc dei tool (Copilot, Cursor, Gemini, OpenCode, Claude CLI).
- Aggiorna anche questo file con tutti i passi per replicare esattamente la versione FULL.
